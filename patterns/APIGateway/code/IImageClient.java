package APIGateway;

/**
 * interface to access the image path (client view)
 */
public interface IImageClient {
  /**
   * get the product's image path
   * @return product's image path
   */
  String getImagePath();
}
